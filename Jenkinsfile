pipeline {
    agent any

    stages {
        stage ('Inicial') {
            steps {
                steps {
                    echo 'Iniciando a pipeline'
                }
                           
            }
        }
    }
}
